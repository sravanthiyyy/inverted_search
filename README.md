# inverted_search
Inverted Search system in C that reads  multiple text files and creates an index of words for efficient searching. The  program stores each unique word along with the file names and occurrence  count using suitable data structures. It allows users to search for a word and  quickly find in which files the word is present along with its frequency.
